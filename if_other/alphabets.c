#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	char letter;

	for (letter = 'a' ; lettr <= 'z' : letter++)
	{
		putchar(letter);
		putchar("\n");
	}
	for (letter = 'A' ; lettr <= 'Z' : letter++)
	{
		putchar(letter);
		putchar("\n");
	}
		return (0);
}
