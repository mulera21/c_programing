#include <stdio.h>

void add(int a, b,sum)
{      
        sum = 0;

	printf("enter two number\n");
	scanf("%d, %d\n", a, b);

	sum = a + b;

	printf("%d + %d = %d\n", a, b, sum);
}

int main(void)
{
	add();
	return (0);
}
